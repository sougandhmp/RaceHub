package org.gce.racehub.home

/**
 * One-time side effects produced by [HomeViewModel]. The shell currently
 * emits no effects of its own; tab-specific effects live alongside their
 * feature ViewModels (RaceEffect, ForumEffect, ProfileEffect).
 */
sealed class HomeEffect
